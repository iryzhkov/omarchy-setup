#!/usr/bin/env bash
# herdr -- terminal workspace manager for AI coding agents.
# In the omarchy repo, so a plain package install on both profiles.
source "${OMARCHY_SETUP_LIB:?}/common.sh"
pkg_install herdr

# Removed on remote (headless) machines only -- GUI apps Omarchy preinstalls
# that are useless without a display.

obsidian
bitwarden

# NOT about vault access: remote hosts do use Bitwarden. Secrets come from the
# vault on every profile (modules/common/70-secrets.sh) via bitwarden-cli,
# which that module installs and nothing removes. Only the desktop app goes.

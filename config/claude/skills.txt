# Skills installed with the Skills CLI (`npx skills add`), globally, for
# Claude Code. One source per line, then the skill names to take from it.
#
#   <owner/repo>   <skill> [<skill> ...]
#
# Installed skills live in ~/.agents/skills/<name> with a symlink from
# ~/.claude/skills/<name>; a name whose symlink exists is skipped. Skills
# written here rather than installed live in config/claude/skills/<name>/ and
# are copied in as owned files. The `omarchy` and `diagnose-crash` skills are
# linked by Omarchy's own migrations and are not listed.
#
# Add-only, like the package lists: a skill installed by hand or dropped from
# this file is never removed by a run.

# Dropped on 2026-09-16, because a skill costs its description line in every
# session: grill-me and grill-with-docs (grilling covers both), handoff and
# implement (thin wrappers over ordinary work and over Jocasta handoffs),
# setup-matt-pocock-skills (a one-time installer) and the issue-tracker suite
# to-spec, to-tickets, triage and wayfinder, unused since it was installed.
# This file is add-only, so dropping a name here stops a reinstall but removes
# nothing; the directories were deleted by hand on the hosts that had them.
mattpocock/skills      code-review codebase-design diagnosing-bugs domain-modeling grilling improve-codebase-architecture prototype resolving-merge-conflicts tdd
juliusbrussee/caveman  caveman
vercel-labs/skills     find-skills

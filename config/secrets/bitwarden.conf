# Non-secret secrets settings. The Bitwarden server URL lives in
# config/defaults.conf (BW_SERVER), alongside the other per-fork values.

# Where rendered environment secrets are written (mode 0600, in a 0700 dir).
SECRETS_ENV_FILE=$HOME/.config/omarchy-setup/secrets.env

# Agent CLIs and dev tools, managed by mise (mise-bin is an Omarchy base
# package, so it is already present).
#
#   <tool>[@<version>]     version defaults to latest
#
# Applied with `mise use --global`, which edits ~/.config/mise/config.toml
# properly rather than this repo hand-editing TOML.

claude              # Claude Code
codex               # OpenAI Codex
antigravity-cli     # agy
pi
opencode
crush

# node backs the npm-installed tools above, so it stays declared even though it
# is not what `node` resolves to at a shell: something pulls in the
# nodejs-lts-jod package, and Omarchy appends the mise shims to PATH *after*
# /usr/bin on purpose, so /usr/bin/node wins. Reach this one with `mise exec
# node -- node` when the version actually matters.
node
gh

# Bitwarden CLI, pinned rather than latest.
#
# Arch's bitwarden-cli (2026.2.0) is older than the API version Vaultwarden
# 1.37.2 advertises (2026.6.0), and unlocking against it failed in
# bitwarden_crypto::keys::master_key with "The provided key is not the expected
# type". Matching the server's API version fixes it. Bump this when the server
# is upgraded.
bitwarden@2026.6.0
